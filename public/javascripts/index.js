$(function() {
  //$(".input").keypress(function (e) {
  //  // User press `Enter` key in input box
  //  if (e.which === 13) {
  //    updatePageLocation();
  //    return false;
  //  }
  //});

  //function createOrUpdateMenu(query, sortBy, onSuccess) {
  //  var endpoint = "/menu";
  //  $.post(endpoint, onSuccess).fail(function (response) {
  //      $("#infobox-content").html(response.responseText);
  //  });
  //}
});
